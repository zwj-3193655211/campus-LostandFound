# 校园失物招领平台 — 项目图表

> 本文档汇总项目的全部架构与流程图表，覆盖系统层级、模块结构、关键业务流程、时序交互、数据库关系、状态机、信息流、部署拓扑等。所有图表均使用 Mermaid 语法，可在 GitHub、IDEA、VSCode 等支持 Mermaid 的环境中直接渲染。

---

## 目录

0. [用例图总览](#0-用例图总览)
1. [系统总览架构图](#1-系统总览架构图)
2. [技术分层架构图](#2-技术分层架构图)
3. [后端模块结构图](#3-后端模块结构图)
4. [前端模块结构图](#4-前端模块结构图)
5. [部署拓扑图](#5-部署拓扑图)
6. [用户认证时序图](#6-用户认证时序图)
7. [物品发布与审核流程图](#7-物品发布与审核流程图)
8. [物品状态机](#8-物品状态机)
9. [智能匹配流程图](#9-智能匹配流程图)
10. [匹配时序图](#10-匹配时序图)
11. [匹配确认与拒绝流程](#11-匹配确认与拒绝流程)
12. [完成申请流程图](#12-完成申请流程图)
13. [实名认证流程图](#13-实名认证流程图)
14. [图片上传流程图](#14-图片上传流程图)
15. [通知与邮件发送流程图](#15-通知与邮件发送流程图)
16. [管理员控制台信息流](#16-管理员控制台信息流)
17. [ER 关系图](#17-er-关系图)

---

## 0. 用例图总览

> 说明：Mermaid 官方未提供 useCaseDiagram，这里用 `flowchart` + 圆角矩形模拟 UML 用例图，渲染效果一致。

### 0.1 系统顶层用例图

```mermaid
flowchart LR
    User([普通用户])
    Admin([管理员])
    SysAdmin([超级管理员])

    subgraph UserUC["普通用户用例"]
        UL((登录系统))
        UR((注册账号))
        UEV((邮箱验证))
        UP1((发布寻物))
        UP2((发布招领))
        UV1((浏览物品))
        UV2((查看匹配))
        UC((确认匹配))
        UJ((拒绝匹配))
        UA((申请完成))
        UVI((实名认证))
        UN((查看通知))
    end

    subgraph AdminUC["管理员用例"]
        AR1((审核物品))
        AR2((审核实名))
        AR3((审核完成))
        AS((查看统计))
        AT((触发匹配))
    end

    subgraph SysUC["超级管理员用例"]
        SM((用户管理))
        SC((系统配置))
    end

    User --> UL
    User --> UR
    User --> UEV
    User --> UP1
    User --> UP2
    User --> UV1
    User --> UV2
    User --> UC
    User --> UJ
    User --> UA
    User --> UVI
    User --> UN

    Admin --> UL
    Admin --> AR1
    Admin --> AR2
    Admin --> AR3
    Admin --> AS
    Admin --> AT
    Admin --> UN

    SysAdmin --> SM
    SysAdmin --> SC

    classDef actor fill:#e3f2fd,stroke:#0d47a1,color:#000
    classDef uc fill:#fff3e0,stroke:#e65100,color:#000
    class User,Admin,SysAdmin actor
    class UL,UR,UEV,UP1,UP2,UV1,UV2,UC,UJ,UA,UVI,UN,AR1,AR2,AR3,AL,AS,AT,SM,SC uc
```

### 0.2 用户角色详细用例图

```mermaid
flowchart LR
    User([普通用户])

    subgraph AccountUC["账号管理"]
        A1((登录))
        A2((注册))
        A3((查看资料))
        A4((编辑资料))
        A5((修改密码))
        A6((重置密码))
        A7((绑定邮箱))
        A8((退出登录))
    end

    subgraph ItemUC["物品管理"]
        I1((查看我的发布))
        I2((编辑物品))
        I3((删除物品))
        I4((标记完成))
    end

    subgraph MatchUC["匹配相关"]
        M1((查看匹配详情))
        M2((查看历史记录))
    end

    User --> A1
    User --> A2
    User --> A3
    User --> A4
    User --> A5
    User --> A6
    User --> A7
    User --> A8
    User --> I1
    User --> I2
    User --> I3
    User --> I4
    User --> M1
    User --> M2

    classDef actor fill:#e3f2fd,stroke:#0d47a1,color:#000
    classDef uc fill:#fff3e0,stroke:#e65100,color:#000
    class User actor
    class A1,A2,A3,A4,A5,A6,A7,A8,I1,I2,I3,I4,M1,M2 uc
```

---

## 1. 系统总览架构图

```mermaid
graph TB
    Client[客户端层 Browser]
    Frontend[前端应用 Vue3+Vite:5173]
    Backend[后端服务 SpringBoot3.2:18090]
    DataLayer[数据层]

    U[用户浏览器]
    A[管理员控制台]

    Client --> U
    Client --> A
    U --> Frontend
    A --> Frontend

    Frontend -->|HTTP/JSON| Backend

    Backend -->|MyBatis-Plus| DataLayer
    Backend -->|SMTP| MailServer[163 邮件服务]
    Backend -->|Sa-Token JWT 无状态| CacheLayer[Redis 缓存]

    DataLayer --> MySQL[(MySQL 8.0)]
    DataLayer --> CacheLayer

    classDef client fill:#e1f5ff,stroke:#01579b
    classDef server fill:#fff3e0,stroke:#e65100
    classDef data fill:#f3e5f5,stroke:#4a148c
    classDef external fill:#e8f5e9,stroke:#1b5e20

    class U,A client
    class Backend,Frontend server
    class MySQL,CacheLayer data
    class MailServer external
```

---

## 2. 技术分层架构图

```mermaid
graph LR
    subgraph L1["表现层（前端）"]
        A1[Vue 3 视图组件]
        A2[Element Plus]
        A3[Vue Router + 守卫]
    end

    subgraph L2["状态与数据层（前端）"]
        B1[Pinia 状态管理]
        B2[Axios HTTP 客户端]
        B3[Token 持久化]
    end

    subgraph L3["API 网关层（后端）"]
        C1[Spring MVC 路由]
        C2[Sa-Token JWT 无状态鉴权]
        C3[全局异常处理]
    end

    subgraph L4["业务服务层（后端）"]
        D1[用户服务]
        D2[物品服务]
        D3[匹配服务]
        D4[通知服务]
        D5[统计服务]
        D6[认证服务]
    end

    subgraph L5["数据访问层（后端）"]
        E1[MyBatis-Plus Mapper]
        E2[Spring Data Redis]
        E3[事务管理]
    end

    subgraph L6["存储层"]
        F1[(MySQL 8.0)]
        F2[(Redis 缓存)]
        F3[(文件存储)]
    end

    A1 --> B1
    A1 --> B2
    B2 -->|HTTP + JWT| C1
    C1 --> C2
    C2 --> D1
    C2 --> D2
    C2 --> D3
    C2 --> D4
    C2 --> D5
    C2 --> D6
    D1 --> E1
    D2 --> E1
    D3 --> E1
    D3 --> E2
    D4 --> E1
    D5 --> E1
    E1 --> F1
    E2 --> F2
    D2 --> F3
```

---

## 3. 后端模块结构图

```mermaid
flowchart TB
    Boot[CampusLostFoundApplication]

    subgraph common["common（公共层）"]
        C1[constant]
        C2[result/ApiResponse]
        C3[exception/GlobalExceptionHandler]
        C4[util/DataMaskUtils]
        C5[dto/request]
    end

    subgraph config["config（框架配置）"]
        CF1[CorsConfig]
        CF2[MybatisMetaObjectHandler]
        CF3[RedisConfig]
        CF4[MailConfig]
        CF5[HttpClientConfig]
        CF6[SwaggerConfig]
        CF7[DataInitializer]
        CF8[MyBatisPlusConfig]
        CF9[WebMvcConfig]
        CF10[AsyncConfig]
    end

    subgraph security["security（认证授权）"]
        S1[WebSecurityConfig]
        S2[SaTokenAuthenticationFilter]
    end

    subgraph commonService["modules/common（公共服务）"]
        CS1[RedisCacheService]
    end

    subgraph systemMod["modules/system（用户/认证）"]
        SY1[AuthController]
        SY2[UserController]
        SY3[EmailVerificationController]
        SY4[AdminUserController]
        SY5[AdminIdentityVerificationController]
        SY6[RootController]
        SY7[AuthService]
        SY8[UserService]
        SY9[EmailVerificationService]
    end

    subgraph itemMod["modules/item（物品相关）"]
        I1[ItemController]
        I2[AdminItemReviewController]
        I3[AdminCompletionRequestController]
        I4[ImageUploadController]
        I5[LocationController（简化版）]
        I6[ItemService]
        I7[ItemCompletionRequestService]
        I8[LocalImageUploadServiceImpl]
    end

    subgraph matchMod["modules/match（智能匹配）"]
        M1[MatchController]
        M2[MatchingServiceImpl]
        M3[MatchingEngine]
        M4[DocumentOwnerMatchServiceImpl]
    end

    subgraph notifMod["modules/notification（通知邮件）"]
        N1[NotificationController]
        N2[NotificationServiceImpl]
        N3[MailServiceImpl]
    end

    subgraph statsMod["modules/statistics（统计）"]
        ST1[PublicStatisticsController]
        ST2[StatisticsController]
        ST3[StatisticsServiceImpl]
    end

    Boot --> C1
    Boot --> CF1
    Boot --> S1
    Boot --> CS1
    Boot --> SY1
    Boot --> I1
    Boot --> M1
    Boot --> N1
    Boot --> ST1

    M2 --> M3
    M2 --> M4
    N3 --> CF4
    I8 --> CF5
    ST3 --> CS1
    SY9 --> CS1
```

---

## 4. 前端模块结构图

```mermaid
graph TB
    subgraph Entry["入口层"]
        E1[main.js 应用启动]
        E2[App.vue 根组件]
        E3[router/index.js 路由配置]
    end

    subgraph Layout["布局层"]
        L1[DefaultLayout 默认布局]
        L2[AdminLayout 管理后台布局]
    end

    subgraph Views["视图层（views/）12 个"]
        V1[Home 首页]
        V2[ItemList 物品列表]
        V3[ItemDetail 物品详情]
        V4[PublishItem 发布物品]
        V5[MatchList 匹配列表]
        V6[MyItems 我的发布]
        V7[Profile 个人中心]
        V8[NotFound 404 页面]
        V9[AdminDashboard 管理员控制台]
        V10[AdminUsers 用户管理]
        V11[AdminIdentityVerifications<br/>实名审核]
        V12[AdminStatistics 数据统计]
    end

    subgraph Components["通用组件（components/）"]
        CP1[ItemCard 物品卡片]
        CP2[MatchCard 匹配卡片]
        CP3[ImageUploader 图片上传]
        CP4[Pagination 分页器]
        CP5[StatusTag 状态标签]
        CP6[EmptyState 空状态]
    end

    subgraph Stores["状态管理（stores/）"]
        ST1[user 用户状态]
        ST2[item 物品状态]
        ST3[app 全局应用状态]
    end

    subgraph Utils["工具层（utils/）"]
        U1[request.js Axios 封装]
        U2[auth.js Token 管理]
        U3[format.js 格式化]
    end

    E1 --> E2
    E2 --> E3
    E3 --> L1
    E3 --> L2
    L1 --> V1
    L1 --> V2
    L1 --> V3
    L1 --> V4
    L1 --> V5
    L1 --> V6
    L1 --> V7
    L2 --> V8
    L2 --> V9
    L2 --> V10
    L2 --> V11
    L2 --> V12

    V2 --> CP1
    V5 --> CP2
    V4 --> CP3
    V3 --> CP1
    V3 --> CP2
    V9 --> CP5
    V2 --> CP4
    V2 --> CP6

    V1 --> ST1
    V1 --> ST2
    V9 --> ST2
    CP1 --> ST2
    U1 --> U2
```

---

## 5. 部署拓扑图

```mermaid
graph TB
    Browser[Chrome / Edge / Safari<br/>localhost:5173]

    subgraph DevHost["开发机（localhost）"]
        ViteDev[Vite Dev Server<br/>127.0.0.1:5173<br/>host=127.0.0.1 强制 IPv4]
        SpringBoot[Spring Boot 3.2<br/>127.0.0.1:18090]
        MySQLProc[MySQL 8.0<br/>127.0.0.1:3306<br/>utf8mb4_unicode_ci]
        RedisProc[Redis 7<br/>127.0.0.1:6379]
        LocalStorage[本地文件存储<br/>./uploads/yyyy/MM/]
    end

    subgraph External["外部依赖"]
        SMTP[163 SMTP<br/>smtp.163.com:465 SSL<br/>发件 19721623703@163.com]
    end

    Browser -->|HTTP 5173| ViteDev
    ViteDev -->|Proxy /api<br/>changeOrigin| SpringBoot
    SpringBoot -->|JDBC<br/>MySQL Connector/J| MySQLProc
    SpringBoot -->|Jedis<br/>Sa-Token 存 token| RedisProc
    SpringBoot -->|文件写入| LocalStorage
    SpringBoot -->|JavaMailSender<br/>SSL 465| SMTP

    classDef browserCls fill:#fff8e1,stroke:#f57f17
    classDef feCls fill:#e3f2fd,stroke:#0d47a1
    classDef beCls fill:#fce4ec,stroke:#880e4f
    classDef dataCls fill:#e8f5e9,stroke:#1b5e20
    classDef extCls fill:#f3e5f5,stroke:#4a148c

    class Browser browserCls
    class ViteDev feCls
    class SpringBoot beCls
    class MySQLProc,RedisProc,LocalStorage dataCls
    class SMTP extCls
```

### 5.1 关键配置

| 配置项 | 值 | 来源 |
|--------|----|------|
| 后端端口 | 18090 | `application.yml: server.port` |
| 前端端口 | 5173 | `vite.config.js: server.port` |
| MySQL 端口 | 3306（默认） | `spring.datasource.url` |
| Redis 端口 | 6379（默认） | `spring.redis.*` |
| SMTP 端口 | 465（SSL） | `spring.mail.properties.mail.smtp.ssl.enable=true` |
| CORS 允许源 | `localhost:3000, 127.0.0.1:3000, :3001, :5173` | `app.cors.allowed-origins` |
| 单文件上传上限 | 10MB | `spring.servlet.multipart.max-file-size` |
| 单请求上限 | 20MB | `spring.servlet.multipart.max-request-size` |
| 本地存储路径 | `./uploads/` | `app.upload.path` |
| Access Token TTL | 7 天 | `sa-token.timeout=604800` |
| JWT 签名密钥 | HS256 ≥32 字节 | `sa-token.jwt-secret-key` |
| DbFix 条件装配 | 默认关闭 | `app.db-fix-enabled=false` |
| 邮箱验证强制 | 默认关闭 | `app.auth.email-verification-required=false` |

### 5.2 端口选择说明

| 端口 | 用途 | 备注 |
|------|------|------|
| 5173 | 前端 Vite | 避开 Win 保留区 |
| 18090 | 后端 Spring Boot | 避开常见端口 |
| 3306 | MySQL | 默认 |
| 6379 | Redis | 默认 |
| 465 | 163 SMTP SSL | 隐式 TLS（非 STARTTLS） |

---

## 6. 用户认证时序图

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户浏览器
    participant F as 前端 Vue
    participant API as 后端 API
    participant Auth as SaToken
    participant DB as MySQL
    participant Cache as Redis

    U->>F: 输入用户名密码
    F->>API: POST /api/auth/login
    API->>Auth: StpUtil.login(userId, access)
    Auth->>Cache: 存 token 信息
    Auth-->>API: accessToken JWT
    API->>DB: 查询用户信息
    DB-->>API: User
    API->>API: 生成 refreshToken
    API->>Auth: StpUtil.login(userId, refresh, 7d)
    API-->>F: 返回 accessToken refreshToken user
    F->>F: 持久化 token 到 localStorage
    F-->>U: 跳转首页

    Note over F,Cache: 后续请求携带 Authorization Bearer
    U->>F: 访问受保护页面
    F->>API: GET /api/users/me
    API->>Auth: 解析 token 获取 loginId
    Auth-->>API: userId
    API->>DB: 查询用户
    DB-->>API: User
    API-->>F: User DTO
    F-->>U: 渲染用户信息
```

---

## 7. 物品发布与审核流程图

```mermaid
flowchart TD
    A[用户访问发布页] --> B{已登录?}
    B -->|否| L[跳转登录页]
    L --> A
    B -->|是| C[填写物品信息]
    C --> D{选择物品类型}
    D -->|LOST| E[填写丢失时间地点]
    D -->|FOUND| F[填写拾取时间地点]
    E --> G[上传物品图片]
    F --> G
    G --> H[提交发布]
    H --> I[前端校验表单]
    I -->|校验失败| J[提示错误] --> C
    I -->|校验通过| K[POST /api/items]
    K --> L2[写入 items 表 status=PENDING]
    L2 --> M[触发智能匹配]
    M --> N[返回物品 ID]
    N --> O[前端跳转详情]
    O --> P{管理员审核}
    P -->|通过| Q[status=APPROVED 公开显示]
    P -->|拒绝| R[status=REJECTED 通知用户]
    Q --> S[触发匹配通知]
    R --> T[发送站内通知]

    style A fill:#e3f2fd
    style Q fill:#c8e6c9
    style R fill:#ffcdd2
```

---

## 8. 物品状态机

```mermaid
stateDiagram-v2
    [*] --> PENDING: 用户发布
    PENDING --> APPROVED: 管理员审核通过
    PENDING --> REJECTED: 管理员审核拒绝
    PENDING --> WITHDRAWN: 用户主动撤回
    APPROVED --> COMPLETED: 申请完成通过
    APPROVED --> WITHDRAWN: 用户主动撤回
    COMPLETED --> [*]

    note right of APPROVED
        匹配成功不会改变物品状态
        物品保持APPROVED状态
    end note
```

---

## 9. 智能匹配流程图

> v2 算法（2026-06 升级）：优化中文文本相似度，支持关键词匹配，阈值调整为 70%。

```mermaid
flowchart TD
    A[物品发布或更新] --> B{状态变更?}
    B -->|是 APPROVED| C[MatchingServiceImpl.match]
    B -->|否| Z[结束]

    C --> D[确定目标类型<br/>LOST→FOUND, FOUND→LOST]
    D --> E[DocumentOwnerMatchService<br/>证件主人匹配 快通道]
    E --> E2[MatchingEngine 评分<br/>候选集约束 + TopK 精排]
    E2 --> F{候选数 > 0?}

    F -->|否| Z
    F -->|是| G[逐个调用 calculateScore]
    G --> G1{串号精确匹配?}
    G1 -->|是| G2[直接 1.00<br/>跳过评分]
    G1 -->|否| H[加权评分<br/>TEXT 35% + 类别 25% + 位置 15% + 品牌 10% + 时间 10% + 颜色 5%]
    G2 --> I
    H --> I{串号冲突?}
    I -->|是| I2[分数 × 0.40 惩罚]
    I -->|否| I3[保持原分]
    I2 --> J
    I3 --> J{分数 >= 0.70?}
    J -->|否| K[丢弃] --> G
    J -->|是| L[写入 matches 表 status=PENDING]
    L --> M[发送匹配通知给双方]
    M --> N{用户确认?}
    N -->|是| O[match.status=CONFIRMED<br/>item.status不变（仍为APPROVED）]
    N -->|否| P[match.status=REJECTED]
    O --> Q[用户可提交完成申请]
    P --> R[记录保持，用户可确认/取消]

    style C fill:#fff3e0
    style E fill:#e8f5e9
    style H fill:#e8f5e9
    style G2 fill:#fff9c4
    style O fill:#c8e6c9
    style P fill:#ffcdd2
```

---

## 10. 匹配时序图

```mermaid
sequenceDiagram
    autonumber
    participant Item as 物品服务
    participant Match as 匹配服务
    participant Algo as 评分算法
    participant DB as MySQL
    participant Notif as 通知服务
    participant User as 用户

    Item->>Match: match(itemId)
    Match->>DB: 查询候选物品（分数>=70%）
    DB-->>Match: List candidates
    loop 每个候选
        Match->>Algo: calculateScore
        Algo-->>Match: BigDecimal score
    end
    Match->>DB: 过滤 score >= 0.70 写入 matches
    DB-->>Match: 匹配记录
    Match->>Notif: 异步发送匹配通知
    Notif->>User: 站内通知 + 邮件
    User->>Match: 查看匹配详情
    alt 用户确认
        User->>Match: confirmMatch
        Match->>DB: 更新 match.status=CONFIRMED
        Match-->>User: 成功
    else 用户拒绝
        User->>Match: rejectMatch
        Match->>DB: 更新 match.status=REJECTED
        Match-->>User: 已拒绝
    end
```

---

## 11. 匹配确认与拒绝流程

```mermaid
flowchart TD
    A[用户收到匹配通知] --> B[点击进入匹配详情]
    B --> C{查看匹配信息}
    C --> D[物品对比]
    D --> E{用户决策}

    E -->|确认匹配| F[POST /api/matches/id/confirm]
    E -->|拒绝匹配| G[POST /api/matches/id/reject]
    E -->|继续浏览| H[返回列表]

    F --> I[match.status=CONFIRMED<br/>item.status不变]
    I --> J[通知对方用户]
    J --> K[用户可提交完成申请]

    G --> M[match.status=REJECTED]
    M --> N[记录保持]

    style F fill:#c8e6c9
    style G fill:#ffcdd2
    style I fill:#c8e6c9
    style M fill:#ffcdd2
```

---

## 12. 完成申请流程图

```mermaid
flowchart TD
    A[匹配确认后] --> B[任一方发起完成申请]
    B --> C[填写原因说明]
    C --> D[POST /api/completion-requests]
    D --> E[写入 item_completion_requests<br/>status=PENDING]
    E --> F[通知对方确认]
    F --> G{对方确认?}

    G -->|同意| H[status=APPROVED]
    G -->|拒绝| I[status=REJECTED]

    H --> K[item.status=COMPLETED]
    K --> L[触发感谢通知]
    L --> M[更新统计 daily_statistics]

    I --> N[item.status恢复APPROVED]
    N --> O[可重新申请]

    style H fill:#c8e6c9
    style I fill:#ffcdd2
    style K fill:#c8e6c9
```

---

## 13. 实名认证流程图

```mermaid
flowchart TD
    A[用户进入认证页面] --> B[填写姓名 学号 身份证]
    B --> C[上传证件照片]
    C --> D[POST /api/identity-verifications]
    D --> E[写入 user_identity_verifications<br/>status=PENDING]
    E --> F[通知校园管理员]

    F --> G{管理员审核}
    G -->|通过| H[status=APPROVED]
    G -->|拒绝| I[status=REJECTED + 原因]

    H --> J[更新 users.identity_status=VERIFIED]
    J --> K[解锁实名相关功能]

    I --> L[用户可重新申请]
    L --> B

    style H fill:#c8e6c9
    style I fill:#ffcdd2
    style J fill:#c8e6c9
```

---

## 14. 图片上传流程图

> 图片存储在本地 `uploads/` 目录，不再使用外部图床。

```mermaid
flowchart TD
    A[用户选择图片] --> B{前端校验}
    B -->|格式或大小不符| C[提示重新选择]
    B -->|通过| D[生成 FormData]

    D --> E[POST /api/uploads/images]
    E --> F[SaToken 鉴权]
    F -->|未登录| F1[返回 401]
    F -->|已登录| G[后端校验]
    G --> G1{大小 ≤ 10MB?}
    G1 -->|否| G2[拒绝]
    G1 -->|是| G3{MIME 命中白名单?<br/>jpg/png/webp/gif}
    G3 -->|否| G3a[拒绝]
    G3 -->|是| G4{扩展名命中白名单?}
    G4 -->|否| G4a[拒绝]
    G4 -->|是| H[生成文件名<br/>UUID + 扩展名]
    H --> I[写入本地 uploads 目录<br/>uploads/yyyy/MM/]
    I --> J[返回本地 URL<br/>/uploads/images/xxx.jpg]
    J --> K[前端回显 + 提交表单携带 URL]

    style E fill:#fff3e0
    style G fill:#fce4ec
    style I fill:#e8f5e9
    style J fill:#c8e6c9
```

---

## 15. 通知与邮件发送流程图

```mermaid
flowchart TD
    A[业务事件触发] --> B{通知类型}
    B -->|站内通知| C[写入 notifications 表]
    B -->|邮件通知| D[检查 user.email_notifications]
    B -->|匹配通知| E[双通道发送]

    C --> F[用户登录后查询]
    D --> G{邮件开关开启?}
    G -->|否| Z[跳过邮件]
    G -->|是| H[构造邮件内容]
    H --> I[JavaMailSender 发送]
    I --> J[163 SMTP 投递]
    J --> K{发送成功?}
    K -->|是| L[标记已发送]
    K -->|否| M[记录失败日志<br/>进入重试队列]

    E --> C
    E --> G

    style I fill:#fff3e0
    style L fill:#c8e6c9
    style M fill:#ffcdd2
```

---

## 16. 管理员控制台信息流

```mermaid
flowchart LR
    subgraph DataSources[数据源]
        D1[(users)]
        D2[(items)]
        D3[(matches)]
        D4[(identity_verifications)]
        D5[(completion_requests)]
        D6[(notifications)]
    end

    subgraph ServiceLayer[服务层]
        S1[StatisticsService]
        S2[ReviewService]
        S3[UserManageService]
        S4[NotificationService]
    end

    subgraph APILayer[API 层]
        A1["/api/admin/stats"]
        A2["/api/admin/reviews"]
        A3["/api/admin/users"]
    end

    subgraph AdminUI[管理员前端]
        U1[Dashboard 仪表盘]
        U2[ReviewList 审核列表]
        U3[UserList 用户列表]
    end

    D1 --> S1
    D2 --> S1
    D3 --> S1
    D2 --> S2
    D4 --> S2
    D5 --> S2
    D1 --> S3
    D6 --> S4

    S1 --> A1
    S2 --> A2
    S3 --> A3
    S4 --> A2

    A1 --> U1
    A2 --> U2
    A3 --> U3

    classDef dataCls fill:#e8f5e9,stroke:#1b5e20,color:#000
    classDef svcCls fill:#fff3e0,stroke:#e65100,color:#000
    classDef apiCls fill:#e3f2fd,stroke:#0d47a1,color:#000
    classDef uiCls fill:#f3e5f5,stroke:#4a148c,color:#000

    class D1,D2,D3,D4,D5,D6 dataCls
    class S1,S2,S3,S4 svcCls
    class A1,A2,A3 apiCls
    class U1,U2,U3 uiCls
```

---

## 17. ER 关系图

```mermaid
erDiagram
    USERS ||--o{ ITEMS : "发布"
    USERS ||--o{ NOTIFICATIONS : "接收"
    USERS ||--o{ USER_IDENTITY_VERIFICATIONS : "提交认证"
    USERS ||--o{ ITEM_COMPLETION_REQUESTS : "发起申请"
    USERS ||--o{ USER_IDENTITY_VERIFICATIONS : "审核"
    USERS ||--o{ ITEM_COMPLETION_REQUESTS : "审核"
    ITEMS ||--o{ MATCHES : "lost_item_id"
    ITEMS ||--o{ MATCHES : "found_item_id"
    ITEMS ||--o{ ITEM_COMPLETION_REQUESTS : "完成申请"

    USERS {
        bigint id PK
        varchar username UK
        varchar password
        varchar email
        varchar phone
        varchar real_name
        varchar id_card UK
        enum identity_status "UNVERIFIED PENDING VERIFIED REJECTED"
        datetime identity_verified_at
        enum role "SUPER_ADMIN CAMPUS_ADMIN USER"
        tinyint status "0禁用 1正常"
        tinyint notification_in_app
        tinyint notification_email
        tinyint notification_match
        tinyint notification_verification
        datetime last_login_time
        datetime created_at
        datetime updated_at
        tinyint deleted "逻辑删除"
    }

    USER_IDENTITY_VERIFICATIONS {
        bigint id PK
        bigint user_id FK
        varchar real_name
        varchar id_card
        enum status "PENDING VERIFIED REJECTED"
        varchar review_reason
        bigint reviewed_by FK
        datetime reviewed_at
        datetime created_at
        datetime updated_at
    }

    ITEMS {
        bigint id PK
        bigint user_id FK
        enum type "LOST FOUND"
        varchar category
        varchar title
        text description
        varchar brand
        varchar color
        varchar location
        datetime lost_time
        datetime found_time
        varchar serial_number
        varchar contact_info
        enum status "PENDING APPROVED REJECTED FOUND_BACK RETURNED EXPIRED CLOSED"
        int view_count
        decimal match_score
        bigint match_item_id
        datetime created_at
        datetime updated_at
        tinyint deleted
    }

    MATCHES {
        bigint id PK
        bigint lost_item_id FK
        bigint found_item_id FK
        decimal score
        enum match_type "SERIAL_EXACT WEIGHTED NONE"
        enum status "PENDING CONFIRMED REJECTED"
        tinyint is_read
        datetime created_at
        datetime updated_at
    }

    ITEM_COMPLETION_REQUESTS {
        bigint id PK
        bigint item_id FK
        bigint user_id FK
        enum target_status "FOUND_BACK RETURNED"
        varchar reason
        enum status "PENDING APPROVED REJECTED"
        varchar review_reason
        bigint reviewed_by FK
        datetime reviewed_at
        datetime created_at
        datetime updated_at
    }

    NOTIFICATIONS {
        bigint id PK
        bigint user_id FK
        enum type "MATCH_FOUND VERIFICATION_RESULT COMPLETION_REVIEW_RESULT SYSTEM"
        varchar title
        text content
        bigint related_id
        tinyint is_read
        datetime created_at
    }

    DAILY_STATISTICS {
        bigint id PK
        date stat_date UK
        int lost_count
        int found_count
        int match_count
        datetime created_at
        datetime updated_at
    }
```

### 17.1 关键约束

- `users.id_card` UNIQUE，承载证件主人精确匹配
- `matches(lost_item_id, found_item_id)` UNIQUE 联合键，防止重复匹配
- `items.serial_number` 建索引，匹配引擎串号快通道使用
- 9 张表全部使用 `utf8mb4_unicode_ci` 排序规则，支持完整 UTF-8 字符集
- 所有外键都带 `ON DELETE CASCADE` 或 `SET NULL`，删除用户/物品时自动清理附属记录
- `items.status` 7 态枚举：`PENDING` → `APPROVED` → `FOUND_BACK` / `RETURNED` / `EXPIRED` / `CLOSED`（详见 §8 / §18 状态机）

### 17.2 字段变更历史

| 阶段 | 变更 | SQL 脚本 |
|------|------|----------|
| Phase 9 | 旧库升级迁移 | `phase9_migration.sql` |
| Phase 10 | 移除 `items.reward` 字段 | `phase10_remove_reward.sql` |
| Phase 17 | 邮箱唯一索引 | `phase17_email_unique.sql` |

---

## 18. 物品状态机（状态流转）

```mermaid
stateDiagram-v2
    [*] --> PENDING: 用户提交发布
    PENDING --> APPROVED: 管理员审核通过
    PENDING --> REJECTED: 管理员审核拒绝
    PENDING --> WITHDRAWN: 用户主动撤回

    APPROVED --> MATCHED: 匹配分数达标且双方确认
    APPROVED --> EXPIRED: 超过 90 天有效期
    APPROVED --> WITHDRAWN: 用户主动撤回

    MATCHED --> COMPLETED: 完成申请通过
    MATCHED --> APPROVED: 匹配被拒绝，物品重新上架
    MATCHED --> EXPIRED: 超过 30 天未完成

    REJECTED --> PENDING: 用户修改后重新提交
    WITHDRAWN --> PENDING: 用户重新发布
    EXPIRED --> [*]
    COMPLETED --> [*]
```

---

## 19. 匹配状态机

```mermaid
stateDiagram-v2
    [*] --> PENDING: 匹配算法生成
    PENDING --> NOTIFIED: 通知发送完成
    NOTIFIED --> CONFIRMED: 用户确认匹配
    NOTIFIED --> REJECTED: 用户拒绝匹配
    NOTIFIED --> EXPIRED: 7 天未处理
    CONFIRMED --> [*]
    REJECTED --> [*]
    EXPIRED --> [*]
```

---

## 20. 实名认证状态机

```mermaid
stateDiagram-v2
    [*] --> UNVERIFIED: 注册默认状态
    UNVERIFIED --> PENDING: 提交认证申请
    PENDING --> APPROVED: 管理员审核通过
    PENDING --> REJECTED: 管理员审核拒绝
    REJECTED --> PENDING: 用户重新提交
    APPROVED --> [*]
```

---

## 21. 完成申请状态机

```mermaid
stateDiagram-v2
    [*] --> PENDING: 发起完成申请
    PENDING --> APPROVED: 对方确认 或 7 天超时
    PENDING --> REJECTED: 对方拒绝
    PENDING --> CANCELLED: 发起方撤回
    REJECTED --> PENDING: 重新发起
    CANCELLED --> [*]
    APPROVED --> [*]
```

---

## 22. 匹配算法评分模型

> v2 实现（2026-05 升级），代码位于 `MatchingEngine.java`。引入文本相似度、平滑时间衰减、串号冲突惩罚。

### 22.1 评分维度与权重

| 维度 | 权重 | 说明 |
|------|------|------|
| TEXT 文本相似度 | 35% | 标题+描述+位置的文本相似度（归一化到 0-1） |
| CATEGORY 类别 | 25% | 相同 +1.0，相似的二级类目 +0.6 |
| LOCATION 位置 | 15% | 同一 location_id +1.0，同区域 +0.5 |
| BRAND 品牌 | 10% | 相同 +1.0，都为空 +0.5 |
| TIME 时间 | 10% | 平滑衰减 exp(-Δt / τ) |
| COLOR 颜色 | 5% | 相同 +1.0，一空 +0.3 |

### 22.2 评分流程

```mermaid
flowchart TD
    Start[calculateScore 入口] --> Serial{串号精确匹配?<br/>trim + uppercase 后相等}
    Serial -->|是| Direct[直接返回 1.00<br/>跳过所有评分]
    Serial -->|否| Dim[计算 6 维子分]

    Dim --> S1[TEXT 文本相似度]
    Dim --> S2[CATEGORY 类别]
    Dim --> S3[LOCATION 位置]
    Dim --> S4[BRAND 品牌]
    Dim --> S5[TIME 时间]
    Dim --> S6[COLOR 颜色]

    S1 --> Weighted[加权求和<br/>保留 2 位小数 HALF_UP]
    S2 --> Weighted
    S3 --> Weighted
    S4 --> Weighted
    S5 --> Weighted
    S6 --> Weighted

    Weighted --> Conflict{串号冲突?<br/>双方都填了但不等的串号}
    Conflict -->|是| Penalize[total × 0.40 惩罚]
    Conflict -->|否| Threshold
    Penalize --> Threshold{分数 >= 0.65?}

    Threshold -->|是| Return[返回分数<br/>生成匹配记录]
    Threshold -->|否| Zero[返回 0<br/>不生成匹配]

    Direct --> End[结束]
    Return --> End
    Zero --> End

    style Direct fill:#fff9c4
    style Penalize fill:#ffcdd2
    style Threshold fill:#fce4ec
    style Return fill:#c8e6c9
    style Zero fill:#eeeeee
```

### 22.3 平滑时间衰减公式

```
score_time = exp(-Δt / τ)
```

- Δt = 双方时间戳差（小时）
- τ = 半衰期常数（默认 168h = 7 天）
- 含义：间隔 7 天的两个物品时间维得分约 0.37，间隔 30 天约 0.11

### 22.4 关键常数

| 常量 | 值 | 含义 |
|------|----|------|
| MATCH_THRESHOLD | 0.65 | 最低匹配分数 |
| SERIAL_CONFLICT_PENALTY | 0.40 | 串号冲突惩罚系数 |
| WEIGHT_TEXT | 0.35 | 文本相似度权重 |
| WEIGHT_CATEGORY | 0.25 | 类别权重 |
| WEIGHT_LOCATION | 0.15 | 位置权重 |
| WEIGHT_BRAND | 0.10 | 品牌权重 |
| WEIGHT_TIME | 0.10 | 时间权重 |
| WEIGHT_COLOR | 0.05 | 颜色权重 |

### 22.5 优化策略

- **候选集约束**：按 `category` + `location` + 时间窗口预过滤，避免全表扫描

- **TopK 精排**：对过滤后的候选按分数排序，仅取 TopK 进入匹配队列

- **串号快通道**：精确匹配直接命中 1.00，跳过评分

- **冲突惩罚**：双方都填了串号但不匹配，强制降权避免误匹配

- **DocumentOwnerMatchService**：基于证件号/学号的主人匹配，单独快通道

  ---

  ## 23. 接口权限矩阵

| 模块 | 接口                                    |  USER   | CAMPUS_ADMIN | SUPER_ADMIN | 匿名 |
| ---- | --------------------------------------- | :-----: | :----------: | :---------: | :--: |
| 认证 | POST /api/auth/register                 |    -    |      -       |      -      |  OK  |
| 认证 | POST /api/auth/login                    |    -    |      -       |      -      |  OK  |
| 认证 | POST /api/auth/logout                   |   OK    |      OK      |     OK      |  -   |
| 用户 | GET /api/users/me                       |   OK    |      OK      |     OK      |  -   |
| 用户 | PUT /api/users/me                       |   OK    |      OK      |     OK      |  -   |
| 用户 | GET /api/admin/users                    |    -    |      OK      |     OK      |  -   |
| 用户 | DELETE /api/admin/users/{id}            |    -    |      -       |     OK      |  -   |
| 物品 | POST /api/items                         |   OK    |      OK      |     OK      |  -   |
| 物品 | GET /api/items                          |   OK    |      OK      |     OK      |  -   |
| 物品 | GET /api/items/{id}                     |   OK    |      OK      |     OK      |  OK  |
| 物品 | PUT /api/items/{id}                     | OK 本人 |      OK      |     OK      |  -   |
| 物品 | DELETE /api/items/{id}                  | OK 本人 |      OK      |     OK      |  -   |
| 物品 | POST /api/admin/items/{id}/approve      |    -    |      OK      |     OK      |  -   |
| 物品 | POST /api/admin/items/{id}/reject       |    -    |      OK      |     OK      |  -   |
| 匹配 | GET /api/matches                        |   OK    |      OK      |     OK      |  -   |
| 匹配 | POST /api/matches/{id}/confirm          |   OK    |      OK      |     OK      |  -   |
| 匹配 | POST /api/matches/{id}/reject           |   OK    |      OK      |     OK      |  -   |
| 匹配 | POST /api/admin/matches/trigger         |    -    |      OK      |     OK      |  -   |
| 完成 | POST /api/completion-requests           |   OK    |      OK      |     OK      |  -   |
| 完成 | POST /api/admin/completion/{id}/approve |    -    |      OK      |     OK      |  -   |
| 认证 | POST /api/identity-verifications        |   OK    |      OK      |     OK      |  -   |
| 认证 | POST /api/admin/identity/{id}/review    |    -    |      OK      |     OK      |  -   |
| 通知 | GET /api/notifications                  |   OK    |      OK      |     OK      |  -   |
| 上传 | POST /api/upload/image                  |   OK    |      OK      |     OK      |  -   |
| 邮箱 | POST /api/auth/email/send               |    -    |      -       |      -      |  OK  |
| 邮箱 | POST /api/auth/email/verify             |    -    |      -       |      -      |  OK  |
| 匹配 | POST /api/admin/matches/{id}/recompute  |    -    |      OK      |     OK      |  -   |
| 完成 | POST /api/admin/completion/{id}/reject  |    -    |      OK      |     OK      |  -   |
| 认证 | GET /api/admin/identity/pending         |    -    |      OK      |     OK      |  -   |
| 系统 | GET /api/admin/dashboard                |    -    |      OK      |     OK      |  -   |
| 系统 | GET /api/public/stats                   |   OK    |      OK      |     OK      |  OK  |
| 统计 | GET /api/admin/stats/overview           |    -    |      OK      |     OK      |  -   |
| 位置 | GET /api/locations                      |   OK    |      OK      |     OK      |  OK  |
| 位置 | POST /api/admin/locations               |    -    |      OK      |     OK      |  -   |

## 24. 数据缓存与一致性

```mermaid
flowchart LR
    subgraph CacheStrategy["缓存策略"]
        C1[用户 Token<br/>Redis TTL = access 30min refresh 7d]
        C2[物品列表查询<br/>Redis Hash TTL = 5min]
        C3[匹配结果<br/>Redis ZSet 按分数排序]
        C4[统计数据<br/>Redis 定时刷新 10min]
        C5[位置字典<br/>Redis Hash TTL = 1h]
    end

    subgraph Consistency["一致性保障"]
        CS1[Cache Aside 模式]
        CS2[写入时先更新 DB<br/>再失效缓存]
        CS3[TTL 自动兜底]
        CS4[分布式锁<br/>防止击穿]
    end

    C1 --> CS1
    C2 --> CS1
    C3 --> CS1
    C4 --> CS1

    CS1 --> CS2
    CS1 --> CS3
    CS1 --> CS4

    style CS1 fill:#c8e6c9
    style CS2 fill:#fff3e0
    style CS4 fill:#ffcdd2
```

缓存更新流程：

```mermaid
sequenceDiagram
    autonumber
    participant Client as 客户端
    participant Service as 业务服务
    participant Cache as Redis
    participant DB as MySQL

    Note over Service,DB: 读流程 Cache Aside
    Client->>Service: 读请求
    Service->>Cache: GET key
    alt 缓存命中
        Cache-->>Service: 返回数据
        Service-->>Client: 响应
    else 缓存未命中
        Service->>DB: SELECT
        DB-->>Service: 数据
        Service->>Cache: SET key value EX 300
        Service-->>Client: 响应
    end

    Note over Service,DB: 写流程
    Client->>Service: 写请求
    Service->>DB: UPDATE INSERT
    DB-->>Service: OK
    Service->>Cache: DEL key
    Service-->>Client: 响应成功
```

---

## 25. 异常处理与降级路径

```mermaid
flowchart TD
    A[请求进入] --> B{统一异常处理}
    B -->|BusinessException| C[业务异常]
    B -->|AuthException| D[认证异常]
    B -->|ValidationException| E[参数校验异常]
    B -->|SQLException| F[数据库异常]
    B -->|UnknownException| G[未知异常]

    C --> H[返回 400 + 业务码]
    D --> I{Token 问题?}
    I -->|过期| J[401 + TOKEN_EXPIRED]
    I -->|无效| K[401 + TOKEN_INVALID]
    I -->|无权限| L[403 + FORBIDDEN]
    E --> M[返回 400 + 字段错误]
    F --> N[记录日志 + 告警]
    G --> N

    H --> O[前端提示]
    J --> O
    K --> O
    L --> O
    M --> O
    N --> P[返回 500 + 通用错误]
    P --> O

    O --> Q{关键路径降级?}
    Q -->|邮件发送失败| R[本地重试队列]
    Q -->|匹配算法超时| S[返回空匹配 + 标记重试]
    Q -->|图片上传失败| T[返回错误 + 建议重试]
    Q -->|Redis 不可用| U[降级直查 MySQL]

    style H fill:#fff3e0
    style J fill:#ffcdd2
    style K fill:#ffcdd2
    style L fill:#ffcdd2
    style P fill:#ffcdd2
    style U fill:#c8e6c9
```

关键降级策略表：

| 故障点 | 检测方式 | 降级行为 | 用户感知 |
|--------|----------|----------|----------|
| Redis 不可用 | 连接超时或异常 | 直查 MySQL | 无感知 性能略降 |
| 邮件服务失败 | SMTP 异常 | 进入重试队列 3 次 | 通知稍后到达 |
| 匹配算法超时 | 超过 5 秒 | 返回空匹配 + 异步重试 | 暂无匹配推荐 |
| 图片上传失败 | IO 异常 | 返回错误码 + 建议 | 提示重试 |
| MySQL 主从延迟 | 读从库时间戳 | 切换到主库读取 | 无感知 |
| Sa-Token 异常 | 401 触发 | 前端自动 refresh token | 静默刷新 |

---

## 26. 邮箱验证流程

> 用于注册时的身份验证、密码重置和敏感操作确认。验证码有效期 10 分钟，每分钟最多发送 1 次。

```mermaid
flowchart TD
    A[用户触发发送 注册/重置密码]
    B[POST /api/auth/email/send]
    C{频率限制<br/>1分钟1次}
    D[生成6位数字验证码]
    E[写入Redis TTL=600s]
    F[JavaMailSender<br/>163 SMTP发送]
    H[返回200]
    I[记录日志<br/>进入重试队列]
    J[用户输入验证码]
    K[POST /api/auth/email/verify]
    L[Redis查询验证码]
    M{存在且匹配?}
    N[400 验证码错误]
    O[标记邮箱已验证]
    P[返回200+token]
    Q[前端跳转下一步]
    R1[返回429<br/>请稍后再试]

    A --> B
    B --> C
    C -->|超限| R1
    C -->|通过| D
    D --> E
    E --> F
    F -->|成功| H
    F -->|失败| I
    H --> J
    I --> J
    J --> K
    K --> L
    L --> M
    M -->|否| N
    M -->|是| O
    O --> P
    P --> Q

    style R1 fill:#ffcdd2
    style I fill:#fff3e0
    style N fill:#ffcdd2
    style P fill:#c8e6c9
```

关键代码：`EmailVerificationController` + `EmailVerificationService`，`MailConfig` 配置 163 SMTP（465 SSL）。

---

## 27. 测试架构

> 经过 17 轮迭代，测试覆盖已覆盖关键链路：单元测试 + 集成测试 + 前端组件测试 + 端到端并发测试。

### 27.1 后端测试分层

```mermaid
graph TB
    subgraph UnitTest["单元测试 src/test/java"]
        U1[ServiceImplTest<br/>业务逻辑]
        U2[ControllerTest<br/>Web 层参数校验]
        U3[MatchingEngineTest<br/>评分算法]
        U4[WebSecurityConfigTest<br/>权限拦截]
    end

    subgraph IntegrationTest["集成测试"]
        I1[ReviewConcurrencyIntegrationTest<br/>真实 H2 + 双线程并发]
        I2[DbFixControllerConditionalTest<br/>条件装配]
    end

    subgraph TestUtils["测试工具"]
        T1[H2 内存数据库<br/>最小建表脚本]
        T2[DataInitializer 重用]
        T3[测试事务回滚]
    end

    UnitTest --> TestUtils
    IntegrationTest --> TestUtils
    IntegrationTest --> H2[(H2 内存库)]

    classDef unit fill:#e3f2fd,stroke:#0d47a1
    classDef integ fill:#fff3e0,stroke:#e65100
    classDef util fill:#e8f5e9,stroke:#1b5e20

    class U1,U2,U3,U4 unit
    class I1,I2 integ
    class T1,T2,T3 util
```

### 27.2 前端测试

- **测试框架**：Vitest（第十七轮引入）
- **测试脚本**：`npm test`
- **覆盖范围**：
  - 路由守卫（未登录跳转登录页、已登录跳转首页）
  - 401 自动 refresh token
  - 表单校验
- **可测试性改造**：`router/index.js` 导出 guard 工厂，`utils/axios.js` 导出 client 工厂

### 27.3 关键测试用例

| 测试类 | 覆盖场景 |
|--------|----------|
| `WebSecurityConfigTest` | 匿名/USER/ADMIN 三角色访问控制 |
| `MatchingEngineTest` | 6 维评分、串号精确匹配、串号冲突惩罚 |
| `VerificationServiceImplTest` | 重复审核拦截、通知失败兜底 |
| `ItemCompletionRequestServiceImplTest` | 完成申请通过/拒绝、并发二次审核拦截 |
| `LocalImageUploadServiceImplTest` | 空文件、超大文件、非法类型、本地存储成功 |
| `DbFixControllerConditionalTest` | `app.db-fix-enabled` 关闭不注册、开启才注册 |
| `ReviewConcurrencyIntegrationTest` | H2 真实事务 + 双线程数据库级并发拦截 |
| `ItemCompletionRequestServiceImplTest` | 关联物品状态漂移保护 |

### 27.4 持续集成命令

```bash
# 后端
cd backend
mvn test                                       # 全量单元 + 集成测试
mvn -Dtest=WebSecurityConfigTest test          # 单类
mvn -DskipTests package                        # 跳过测试打包

# 前端
cd frontend
npm test                                       # Vitest 单元测试
npm run build                                  # 生产构建
```

---

## 28. 并发审核与状态漂移保护

> 第十五、十六轮加强：审核改为按 `id + PENDING` 条件原子更新，缩小"先查后改"窗口。

```mermaid
sequenceDiagram
    autonumber
    participant A as 管理员 A
    participant B as 管理员 B
    participant API as 后端 API
    participant DB as MySQL

    par 并发请求
        A->>API: POST /approve
        B->>API: POST /approve
    end

    API->>DB: UPDATE matches<br/>SET status=APPROVED<br/>WHERE id=? AND status='PENDING'
    DB-->>API: A 更新 1 行
    DB-->>API: B 更新 0 行
    API-->>A: 200 成功
    API-->>B: 409 已被审核
    B->>B: 提示"该申请已被处理"

    Note over API,DB: 关联物品状态漂移保护
    API->>DB: UPDATE items<br/>SET status=COMPLETED<br/>WHERE id=? AND status='MATCHED'
    alt 物品状态已变
        DB-->>API: 0 行受影响
        API->>API: 事务回滚
        API-->>A: 409 关联物品状态已变更
    else 正常完成
        DB-->>API: 1 行受影响
        API-->>A: 200 成功
    end
```

关键代码：`VerificationServiceImpl.confirmVerification`、`ItemCompletionRequestServiceImpl.approve`，使用 `LambdaUpdateWrapper` + `eq("id", id).eq("status", "PENDING")` 实现条件原子更新。

---

> 文档版本 v2.0  最后更新 2026-06-08  合并 17 轮迭代变更
