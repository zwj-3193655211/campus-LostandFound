spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:campus_lostfound_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 100ms
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  mail:
    host: localhost
    port: 2525
    username: test@example.com
    password: test-password


app:
  base-url: http://localhost:8081
  init-demo-data: false

sa-token:
  token-name: Authorization
  timeout: 900
  is-concurrent: true
  token-prefix: Bearer
  jwt-secret-key: test-sa-token-jwt-secret-32bytes-min-required-for-hs256-algorithm
