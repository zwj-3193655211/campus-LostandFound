# 项目接手计划

## 目标
- 全面扫描项目，识别当前明显错误与高风险设计问题。
- 重点核查权限设计、数据库与后端实现一致性、硬编码问题、前端登录功能问题。
- 在需求明确后逐步修复，并保留可追踪的分析与验证记录。

## 阶段
| 阶段 | 状态 | 说明 |
|---|---|---|
| 1 | 已完成 | 建立计划与项目初扫 |
| 2 | 已完成 | 后端权限、数据库、配置核查 |
| 3 | 已完成 | 前端登录、路由、接口交互核查 |
| 4 | 已完成 | 需求分析与不确定项确认 |
| 5 | 已完成 | 实施修复 |
| 6 | 已完成 | 构建、测试、回归验证 |
| 7 | 已完成 | 代码审查、测试回归与实现文档补齐 |
| 8 | 已完成 | 前端 Vitest 基线与关键守卫测试 |
| 9 | 进行中 | 智能匹配算法升级 + 移除悬赏金设计 |

## 当前关注点
- 是否存在权限边界设计错误。
- SQL 初始化脚本与实体/服务实现是否不一致。
- 是否存在账号、角色、接口地址、密钥等硬编码。
- 登录态存储、请求拦截、路由守卫、角色访问是否异常。
- JWT 过滤器是否真正按 `access token` 和数据库当前角色生效。
- 新确认的业务规则是否已映射到数据库状态、审核流、匹配标签和图片上传链路。
- 智能匹配是否能利用标题/描述等文本信息，降低同类物品的误匹配率，并保持性能可控。
- 移除“悬赏金”字段后，前后端接口与 SQL 基线是否一致，且不引入兼容性回归。

## 已确认决策
- 管理后台允许 `SUPER_ADMIN` 与 `CAMPUS_ADMIN` 访问。
- `/api/db/**` 仅允许 `SUPER_ADMIN` 访问，不再公开。
- 初始化最终统一为一套 SQL 脚本。
- 本轮优先尽量覆盖安全、登录、测试和主要不一致问题。
- 游客仅可浏览与搜索；登录后才可发布、匹配、认领和提交完成申请。
- 物品发布需审核通过后公开。
- `已匹配` 为高分匹配标签，不占用物品主状态；阈值固定为 `80%`。
- 完成状态采用“寻物已找到 / 招领已归还”，由用户提交申请、审核员审批后生效。
- 图片采用图床上传，默认存储目的地为 `r2`。
- 智能匹配升级优先采用“规则剪枝 + 文本相似度精排”的可解释方案，不引入外部向量/LLM 依赖。
- “悬赏金”字段从产品层移除：不再提供单独输入与展示，仅允许用户在描述中自愿写感谢/酬谢信息。
- 历史数据库升级优先使用显式迁移脚本，避免完全依赖运行时自动修表。
- 一键启动需要覆盖数据库迁移、后端构建、前端启动和基本可用性验证，不能只启动后端。

## 错误记录
| 问题 | 尝试 | 结果 |
|---|---|---|
| `WebSecurityConfigTest` 初版因 `@MapperScan` 带入 MyBatis Repository 无法启动 | 1 | 在测试中补齐相关 Repository 的 `@MockBean` 后解决 |
| 管理员用户列表接口在权限测试中返回 500 | 1 | 确认根因是多个控制器 `@RequestParam` 未显式声明参数名，已修复 |
| 需要进一步验证真实 JWT 链路而不是只做 `WebMvcTest` | 1 | 已补 `JwtSecurityIntegrationTest`，验证通过 |
| `mvn -Dtest=a,b` 在 PowerShell 中被解析为参数列表 | 1 | 改为 `mvn "-Dtest=a,b"` 后解决 |
| 匹配服务重构时把 `lostItemId/foundItemId` 的声明放到了去重逻辑后面 | 1 | 调整变量声明顺序后恢复编译通过 |
| 一键启动后前端页面空白、接口 `ECONNREFUSED` | 1 | 确认根因是后端原 `start ... cmd /k` 嵌套命令过脆，改为独立 `启动后端.bat/启动前端.bat` 后恢复 |
| `phase9_migration.sql` 初版使用 `ADD COLUMN IF NOT EXISTS` 在当前 MySQL 环境报语法错误 | 1 | 改为 `information_schema + PREPARE` 的兼容写法后解决 |
| `WebSecurityConfigTest` 在新增实名认证仓库后上下文加载失败 | 1 | 为 `UserIdentityVerificationRepository` 补 `@MockBean` 后恢复通过 |
| `DbFixController` 条件装配属性名写成 `app.db-fix.enabled`，与配置 `app.db-fix-enabled` 不一致 | 1 | 改为 `@ConditionalOnProperty(prefix = "app", name = "db-fix-enabled", havingValue = "true")` 后恢复 |
| PowerShell 中执行 `mvn "-DskipTests package"` 会把参数整体传入，导致 Maven 提示未指定 goal | 1 | 改为 `mvn -DskipTests package` 后打包通过 |
